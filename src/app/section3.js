import FadingDiv from "@/components/visual/fading-div";

const Section3 = () => {
  return (
    <section className="section bg-gradient-to-b from-myPurple to-myRed text-white snap-start">
      <FadingDiv section={3} />
    </section>
  );
};

export default Section3;
